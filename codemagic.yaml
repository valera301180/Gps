workflows:
  android-workflow:
    name: Android Build
    max_build_duration: 30
    environment:
      groups:
        - default
    scripts:
      - name: Build Android APK
        script: |
          gradle assembleDebug
    artifacts:
      - app/build/outputs/**/**/*.apk
